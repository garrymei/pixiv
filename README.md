# 粤次元君 (Yue Ci Yuan Jun)

## 一、项目概述

“粤次元君”是一款面向粤港澳大湾区二次元爱好者的社交创作社区小程序。

平台聚焦“内容创作 + 线下协作 + 活动连接”三大核心场景，连接：

- Coser
- 摄影师
- 妆娘 / 毛娘 / 道具师
- 二次元爱好者

构建一个既能看内容，又能约合作 / 找人 / 参与活动的垂直社区。

## 二、产品定位

🎯 大湾区二次元社交创作社区

区别于泛社交产品，本项目强调：

- 真实创作协作（约拍 / 求妆 / 拼团）
- 线下活动连接（漫展 / 同人 / 外拍）
- 内容 + 关系 + 行动闭环

## 三、当前状态（非常重要）

项目已完成从 0 → MVP → 可运行版本的完整建设：

| 阶段 | 名称 | 状态 |
| --- | --- | --- |
| 阶段0 | 项目定向 | ✅ 已完成 |
| 阶段1 | 产品 & 技术设计 | ✅ 已完成 |
| 阶段2 | 前端骨架 | ✅ 已完成 |
| 阶段3 | 后端核心能力 | ✅ 已完成 |
| 阶段4 | 前后端联调 | ✅ 已完成 |
| 阶段5 | 测试与上线准备 | ✅ 已完成 |
| 阶段6 | 冷启动与运营准备 | ✅ 已完成 |

🚀 当前阶段重点（不是开发）

项目已进入：真实运行 / 冷启动阶段

当前优先级：

- 内容供给（帖子 / 需求 / 活动）
- 种子用户引入
- 用户行为与反馈收集
- 核心链路优化（发布 / 报名 / 参与）

## 四、MVP 功能范围

✅ 当前已支持

#### 1. 内容模块

- 图文作品发布（正片）
- 日常动态发布
- 点赞 / 评论

#### 2. 活动模块

- 活动资讯列表
- 活动详情
- 活动报名

#### 3. 需求协作模块

- 发布协作需求（摄影 / 模特 / 妆造 / 毛发 / 道具）
- 参与需求
- 需求列表与筛选

#### 4. 用户模块

- 登录（当前为 Mock）
- 个人资料
- 我的发布 / 我的活动 / 我的需求

## 五、当前明确不做（重要）

为保证项目不跑偏，当前阶段不做：

- ❌ 私信 / 聊天系统
- ❌ 推荐算法
- ❌ 电商 / 交易系统
- ❌ 复杂会员体系
- ❌ 消息推送（订阅 / Push）
- ❌ 私信 / 聊天系统
- ❌ 复杂会员体系
- ❌ 泛社交扩展（非二次元方向）

## 六、核心业务链路（建议重点看）

🎨 内容主线

登录 → 首页 → 帖子详情 → 点赞 / 评论 → 发布作品

🎫 活动主线

活动列表 → 活动详情 → 报名 → 我的活动

🤝 需求主线

需求列表 → 需求详情 → 参与 → 我的需求

👤 用户主线

我的页面 → 编辑资料 → 查看个人内容

## 七、项目结构

```text
src/
  pages/            # 页面（首页/活动/需求/我的）
  components/       # 通用组件（Card/Feed 等）
  services/         # API 封装
  styles/           # 设计 Token 与样式
  mocks/            # Mock 数据

config/             # Taro 环境配置（dev / prod / index）
dist/               # 小程序构建产物

backend/
  src/
    modules/        # 用户/内容/活动/需求模块
    common/         # 公共组件（中间件/工具）
    config/         # 配置文件

docs/
  product/          # 页面规格 & 产品设计
  backend/          # API & 数据库设计
  testing/          # 测试文档
  release/          # 发布准备
  launch/           # 冷启动 & 运营策略
```

## 八、技术选型

### 前端

- Taro 4
- React 18
- 微信小程序
- Sass

### 后端

- Node.js
- NestJS
- RESTful API

### 数据库

- MySQL 8.0
- `mysql://[DB-USERNAME]:[DB-PASSWORD]@sh-cynosdbmysql-grp-9k31bzlm.sql.tencentcdb.com:27853/demo-1gp5eo6c4e09685e`

### 可选

- Redis（缓存）
- 对象存储（图片）

## 九、数据模型（核心表）

| 表名 | 描述 |
| --- | --- |
| `sys_user` | 用户 |
| `post_works` | 正片作品 |
| `post_daily` | 日常动态 |
| `event_info` | 活动 |
| `event_enroll` | 活动报名 |
| `collab_req` | 协作需求 |
| `collab_apply` | 需求参与 |

## 十、快速启动（Getting Started）

### 前端

```bash
cd /home/ubuntu/Documents/pixiv
npm install
npm run dev:weapp
```

使用微信开发者工具打开项目根目录：

```text
/home/ubuntu/Documents/pixiv
```

如需产出一次性构建文件：

```bash
npm run build:weapp
```

### 后端

```bash
cd backend
npm install
npm run start:dev
```

### 环境变量示例

```env
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
JWT_SECRET=
UPLOAD_BASE_URL=
API_BASE_URL=
```

## 十一、文档入口

| 类型 | 路径 |
| --- | --- |
| 页面设计 | `docs/product/` |
| API 文档 | `docs/backend/api-spec-v1.md` |
| 数据库设计 | `docs/backend/database-design-v1.md` |
| 测试文档 | `docs/testing/` |
| 发布准备 | `docs/release/` |
| 冷启动方案 | `docs/launch/` |

## 十二、后台平台需求（新增）

当前项目已不再满足“只有小程序前台”的阶段，后续需要建设一个与小程序模块逐一对应的后台管理与监控平台。

建设目标：

- 每个小程序业务模块都有一个对应的后台管理入口
- 每个小程序业务模块都有一个对应的监控入口
- 后台既能做内容运营，也能做平台运维与异常排查
- 小程序端、后台 Web、后端 API 共用同一套业务数据模型

### 1. 后台平台设计原则

- 一个前台模块，对应一个后台模块
- 一个后台模块，至少包含：管理区、数据看板、异常监控、操作日志
- 管理能力与监控能力并行建设，不能只做内容录入页
- 优先围绕当前已上线模块建设，不空转做泛后台

### 2. 小程序模块 -> 后台平台映射

| 小程序模块 | 当前前台能力 | 后台管理能力 | 后台监控能力 |
| --- | --- | --- | --- |
| 首页 | Banner / 推荐流 / 活动入口 | 首页推荐位、Banner、排序规则、公告配置 | 首页接口成功率、首屏耗时、空数据告警、点击率 |
| 发现 | 动态广场、次元市集、标签筛选 | 内容管理、需求管理、标签管理、推荐词配置 | 搜索成功率、空结果率、列表空数据率、Mock 兜底次数 |
| 发布 | 发布帖子、发布需求 | 帖子审核、需求审核、敏感词、发布频控、失败日志 | 发帖成功率、需求发布成功率、上传失败率、慢请求排行 |
| 活动 | 活动列表、详情、报名、我的活动 | 活动 CRUD、是否可报名、报名名单、状态管理、核销签到 | 报名成功率、报名失败原因、满额告警、截止告警、活动转化率 |
| 我的/用户 | 资料编辑、我的帖子、我的需求、我的活动 | 用户档案、用户状态、内容总览、违规记录、封禁管理 | 资料修改成功率、异常账号识别、高频操作告警 |
| 登录会话 | 登录页、Mock 登录、Token 会话 | 登录记录、会话配置、账号策略、黑名单 | 登录成功率、401/403 统计、会话初始化失败率 |
| 上传资源 | 图片上传、封面读取、资源展示 | 上传文件管理、资源审查、失效资源清理 | 上传成功率、资源 404、上传耗时、非法资源告警 |
| 系统运维 | `/health`、域名、Nginx、后端服务 | 服务配置、告警中心、操作审计、运行配置 | 服务可用性、5xx 错误率、systemd 状态、证书到期提醒 |

### 3. 活动模块专项要求

活动模块必须区分：

- 资讯类活动：仅展示，不开放报名
- 可参加活动：支持报名、报名状态查询、报名名单查看

当前已落地字段：

- `event_type`
- `is_registerable`

当前已落地后台能力：

- Web 后台入口：`/admin`
- 活动创建 / 编辑 / 删除
- 是否可报名开关
- 报名名单联动查看

后续待补：

- 活动核销 / 签到
- 报名状态筛选
- 活动数据统计
- 活动操作审计

### 4. 后台平台建设阶段

#### 阶段 A：平台骨架

- 后台首页驾驶舱
- 模块导航
- 活动管理从单页升级为平台内一个模块
- 各业务模块的占位视图与基础状态提示

#### 阶段 B：业务管理完善

- 首页配置
- 发现内容与需求管理
- 发布审核
- 用户管理
- 登录与会话管理
- 上传资源管理

#### 阶段 C：监控与告警

- 模块级指标面板
- 异常日志列表
- 慢请求与错误率统计
- systemd / Nginx / 健康检查联动
- 告警通知（企业微信 / 飞书 / 邮件）

#### 阶段 D：权限与审计

- 后台账号体系
- 角色权限（super_admin / operator / auditor / viewer）
- 操作审计日志
- 配置变更追踪

### 5. 当前开发约束

- 后端服务通过 `pivix-backend.service` 运行，生产入口为 `backend/dist/main.js`
- 域名统一使用 `https://www.pivix.top`
- 反向代理由 Nginx 负责，`443 -> 3000`
- `/health` 为服务健康检查标准接口
- `/admin` 为后台平台统一入口，后续不再仅限“活动管理页”
- 当前数据库连接目标：
  - `DB_HOST=sh-cynosdbmysql-grp-9k31bzlm.sql.tencentcdb.com`
  - `DB_PORT=27853`
  - `DB_NAME=pivix`
  - 实际运行配置文件：`backend/.env`
  - 密码与敏感凭据不写入文档，统一从环境变量读取

## 十三、已知问题 / 当前限制

- 当前前台登录仍以 Mock 会话为主（默认 `mockId=dev`）
- 上传能力为基础实现（后续可接云存储）
- 无举报 / 收藏 / 关注能力
- 管理后台已上线基础版本，但仍处于“平台骨架建设阶段”
- 未做推荐排序（时间流）
- 当前接口统一指向 `https://www.pivix.top`

## 十四、当前优先级（非常关键）

❗如果你是开发者，请优先做这些，而不是加新功能：

- 发布流程优化（更顺）
- 活动报名体验优化
- 需求参与转化优化
- 首页内容丰富度提升
- Bug 修复（优先 P0 / P1）

## 十五、下一阶段方向（Roadmap）

### V1.1

- 收藏
- 关注
- 标签优化
- 举报入口

### V1.2

- 内容推荐优化（轻量）
- 活动承接增强
- 用户留存机制

## 十六、开发规范

### 分支

- `main`：生产
- `develop`：开发
- `feature/*`：功能

### 提交

- `feat:` 新功能
- `fix:` 修复问题
- `docs:` 文档更新

## 十七、项目原则（非常重要）

- 不做泛社交
- 不做复杂系统
- 不做重功能扩展
- 优先保证“内容 + 协作 + 活动”闭环
