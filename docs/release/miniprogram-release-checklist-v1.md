# 粤次元君 - 微信小程序发布配置检查清单 V1

## 一、目标

在提交微信小程序审核前，先把容易被忽略但会直接卡审核或卡发布的配置项整理清楚，避免出现“代码可用但发布失败”的情况。

## 二、当前项目基线

基于当前仓库检查结果：

- 前端主配置位于 `frontend/src/app.config.ts`
- 域名配置位于 `frontend/src/config/env.ts`
- 图片上传调用位于 `frontend/src/services/uploads.ts`
- 生产环境当前指向 `http://43.167.164.162:3000`
- 本地开发环境仍使用 `http://43.167.164.162:3000`，仅可用于开发，不能作为发布配置
- 当前未发现 `downloadFile` 调用
- 当前 `tabBar` 已有 5 个入口，但未配置图标资源
- 当前前端未发现“隐私协议 / 用户协议 / 举报入口”页面或跳转实现

## 三、发布前必查项

### 3.1 合法域名

| 项目 | 当前情况 | 发布要求 | 建议结论 |
| --- | --- | --- | --- |
| request 合法域名 | 生产环境 API 基址为 `http://43.167.164.162:3000` | 必须在微信公众平台后台配置到“request 合法域名” | 将 `43.167.164.162:3000` 加入 request 白名单，并确认正式发布时是否切换为生产域名 |
| uploadFile 合法域名 | 上传接口走 `resolveApiUrl('/uploads/image')`，实际与 API 同域 | 必须在微信公众平台后台配置到“uploadFile 合法域名” | 将 `43.167.164.162:3000` 同步加入 uploadFile 白名单 |
| downloadFile 合法域名 | 代码中暂未使用 `Taro.downloadFile` | 若后续接入文件下载、海报保存、音视频缓存，需单独配置 | 当前可标记为“暂不使用”；若后续改为 COS / OSS 下载，需补登记 |

### 3.2 HTTPS 配置

| 检查项 | 当前情况 | 风险 | 建议 |
| --- | --- | --- | --- |
| 生产接口 HTTPS | 已使用 `http://43.167.164.162:3000` | 低 | 保持 HTTPS，证书需有效且完整 |
| 本地接口 HTTP | `http://43.167.164.162:3000` | 高 | 仅用于开发调试，不能出现在提审与正式版环境 |
| 上传接口 HTTPS | 与 API 同域，生产为 HTTPS | 低 | 发布前实测 1 次图片上传 |
| 跳转链接 HTTPS | 若后续增加协议页、活动外链、客服页 | 中 | 所有外链统一使用 HTTPS |

发布建议：

- 提审包只保留 HTTPS 域名配置，不要混入本地 HTTP 配置
- 服务端证书需由可信 CA 签发，避免中间证书链不完整
- 若使用对象存储承载图片，也要确认图片访问域名同样为 HTTPS

### 3.3 页面路径有效性

当前主包页面路径已核对，核心页面均存在对应页面文件，未发现明显失效路由。

| 页面 | 状态 |
| --- | --- |
| `pages/home/index` | 有效 |
| `pages/discover/index` | 有效 |
| `pages/publish/index` | 有效 |
| `pages/events/index` | 有效 |
| `pages/profile/index` | 有效 |
| `pages/post-detail/index` | 有效 |
| `pages/publish-post/index` | 有效 |
| `pages/publish-demand/index` | 有效 |
| `pages/demand-detail/index` | 有效 |
| `pages/event-detail/index` | 有效 |
| `pages/my-posts/index` | 有效 |
| `pages/my-events/index` | 有效 |
| `pages/my-demands/index` | 有效 |
| `pages/my-applied-demands/index` | 有效 |
| `pages/edit-profile/index` | 有效 |

发布前补充检查：

- 轮播图、按钮、卡片跳转路径全部走一遍，避免参数页存在“可配置但未联通”的情况
- 检查首页 Banner 链接是否全部指向真实页面
- 检查“我的页面”菜单点击后是否都能正常进入目标页

### 3.4 tabBar 配置

当前 `tabBar` 文案如下：

| 入口 | 页面路径 | 文案 | 当前状态 |
| --- | --- | --- | --- |
| 首页 | `pages/home/index` | 首页 | 正常 |
| 发现 | `pages/discover/index` | 发现 | 正常 |
| 发布 | `pages/publish/index` | 发布 | 正常 |
| 活动 | `pages/events/index` | 活动 | 正常 |
| 我的 | `pages/profile/index` | 我的 | 正常 |

当前缺口：

- 未配置 `iconPath`
- 未配置 `selectedIconPath`

建议：

- 审核版必须补齐 tabBar 图标，避免视觉完成度过低
- 图标风格建议统一为深色背景下的线性图标
- “发布”入口建议做视觉强化，但不要过度像广告按钮
- 文案保持简洁，不建议改成过长表达

### 3.5 小程序名称 / 简介 / 类目建议

#### 名称建议

- 首选：粤次元君
- 备选：粤次元圈 / 粤次元同好

名称要求：

- 避免使用夸张、绝对化、带诱导性质的词
- 若名称含“官方”字样，需确认主体资质和实际运营身份匹配

#### 简介建议

可选版本：

1. 粤港澳二次元同好交流社区，支持发帖、招募、活动资讯与官方活动报名。
2. 面向华南地区 Coser、摄影、妆造同好的轻量社区，可看内容、发需求、报活动。
3. 粤次元君是一个二次元兴趣交流平台，提供作品分享、同好招募、活动资讯和报名能力。

简介原则：

- 描述真实功能，不写“最大”“最全”“官方唯一”等高风险表述
- 要与当前 MVP 功能一致，不能写尚未开放的能力

#### 类目建议

推荐优先顺序：

1. 社交
2. 工具 > 社区/信息服务相关子类目
3. 文娱 > 动漫/兴趣社区相关子类目

执行建议：

- 最终以微信公众平台可选类目为准
- 选择最贴近“兴趣交流 + 活动信息 + UGC 社区”的类目
- 类目说明、简介、截图、实际页面内容必须一致

### 3.6 隐私协议 / 用户协议入口预留

当前状态：

- 前端暂未发现“隐私政策”“用户协议”页面或入口

发布建议：

- 在“我的”页面底部预留两个入口：
  - 隐私政策
  - 用户协议
- 如当前没有独立 H5，可先接入占位页，但提审前必须替换为正式内容
- 登录、注册、发布前如涉及个人信息处理，应确保用户可找到隐私说明

建议最少落点：

- 我的页底部
- 登录授权弹窗说明区域
- 发布页底部或发布须知区

### 3.7 必要权限说明

当前项目已明确涉及的权限与能力：

| 能力 | 当前使用情况 | 提审说明建议 |
| --- | --- | --- |
| 相册 / 图片选择 | 发布帖子时使用 `Taro.chooseImage` | 用于用户上传作品图片、活动图片 |
| 用户身份信息 | 当前为 mock 登录，后续若接微信登录需补充 | 若改为微信授权登录，需明确说明昵称头像用途 |
| 文件上传 | 已使用 | 用于帖子图片上传 |

当前未发现必须声明但已实际调用的权限：

- 地理位置
- 通讯录
- 麦克风
- 摄像头

建议：

- 没有实际用到的权限不要申请
- 如果后续新增“保存海报到相册”“扫码报名”等能力，要同步补权限说明

## 四、审核容易被卡的点

### 4.1 配置类风险

- request / uploadFile 域名未在后台白名单配置
- 正式环境仍指向本地或测试 HTTP 地址
- tabBar 缺图标或资源路径错误
- Banner、活动详情、我的页面存在死链或空白页

### 4.2 内容类风险

- 小程序名称、简介和真实功能不一致
- 介绍写了社交、扩列、活动报名，但页面没有对应闭环
- UGC 社区没有隐私协议、用户协议、举报入口、下架机制说明
- 首页出现测试占位内容、失效活动、异常昵称或不合规文案

### 4.3 审核体验风险

- 发布入口明显可用，但没有最基本内容治理说明
- 活动报名、发布需求、发帖后没有反馈或失败提示不明确
- “官方活动”命名与实际主体不匹配，容易被追问资质

## 五、发布前最终 Checklist

- [ ] 微信公众平台已配置 request 合法域名
- [ ] 微信公众平台已配置 uploadFile 合法域名
- [ ] 当前版本如无 `downloadFile`，已明确标记“暂不使用”
- [ ] 提审环境全部使用 HTTPS
- [ ] 本地 HTTP 地址未出现在提审配置中
- [ ] 15 个页面路径全部可打开
- [ ] Banner、卡片、快捷入口、我的菜单均无失效跳转
- [ ] tabBar 图标已补齐，文字无歧义
- [ ] 小程序名称、简介、类目与实际功能一致
- [ ] 我的页已预留隐私政策与用户协议入口
- [ ] 发布相关页面已有最少权限与用途说明
- [ ] UGC 相关风险点已有最小应对方案
- [ ] 首页、活动页、需求页已替换为可展示内容，不出现空站感

## 六、建议优先级

### P0：提审前必须补齐

- request / uploadFile 域名后台白名单
- HTTPS 正式域名确认
- 隐私政策 / 用户协议入口
- tabBar 图标资源

### P1：建议提审前完成

- Banner 跳转与页面链路回归
- 名称 / 简介 / 类目统一口径
- 发布、上传、报名失败提示文案复核

### P2：上线后尽快完善

- downloadFile 预留域名策略
- 协议页内容精修
- 审核截图与演示数据优化

## 七、可直接丢给 Trae 的 Prompt

请帮我检查微信小程序发布前配置，项目名是“粤次元君”。

要求输出一份发布检查结论，至少包含：

1. request / uploadFile / downloadFile 合法域名检查
2. HTTPS 配置检查
3. 页面路径有效性检查
4. tabBar 图标与文字检查
5. 小程序名称 / 简介 / 类目建议
6. 隐私协议 / 用户协议入口预留建议
7. 审核容易被卡的风险点
8. 一份可执行的发布前 checklist

请结合当前项目代码结构，明确指出“已满足”“待补齐”“存在风险”的项。
